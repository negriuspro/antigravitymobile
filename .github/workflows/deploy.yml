name: Deploy antigravitymobile

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to angel-server
    runs-on: [self-hosted, linux, antigravitymobile]
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Deploy
        id: deploy
        run: bash /home/angel/deploy/deploy-antigravitymobile.sh

      - name: Verify containers
        if: always()
        run: |
          echo "--- Estado de contenedores ---"
          docker ps --filter name=antigravitymobile --format "table {{.Names}}	{{.Status}}	{{.Ports}}"

      - name: Health check final
        run: |
          sleep 3
          if nc -z 127.0.0.1 3001 2>/dev/null; then
            echo "OK — Puerto :3001 responde"
          else
            echo "WARN — Puerto :3001 no responde (puede ser normal en hairpin NAT)"
          fi

      - name: Deploy log (ultimas lineas)
        if: always()
        run: tail -20 /home/angel/deploy/deploy.log || true
